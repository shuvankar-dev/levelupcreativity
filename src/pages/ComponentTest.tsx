import React from 'react';
import WhatWeCreateUI from '../components/WhatWeCreateUI';

const ComponentTest: React.FC = () => {
  return (
    <div style={{ background: '#000000', minHeight: '100vh', padding: '40px 0' }}>
      <WhatWeCreateUI />
    </div>
  );
};

export default ComponentTest;
